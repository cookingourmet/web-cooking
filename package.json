{
  "name": "web-cooking",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && vite build --ssr src/prerender.ts --outDir .prerender && node scripts/prerender.mjs && node scripts/validar_actualizacion_noviembre.mjs",
    "preview": "vite preview",
    "verify:november": "node scripts/validar_actualizacion_noviembre.mjs"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "resend": "^6.12.3"
  }
}
